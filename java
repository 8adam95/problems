			      WEEEK 7


1.It will print sum of the integers given in the terminal.
2.I think it really depends on the case, for example in this code exceptions are confusing.
3. for(int i = 0; i < args.length; i++)
4. e.g. if we do not give integers in the command line
5.  import java.util.Scanner
    Scanner in = new Scanner(System.in);
    try
    {
      int a = in.nextInt();
    }
    catch(Exception e)
    {
      System.out.println("Not an integer");
    }

6. try if initialDep > 10 exception

